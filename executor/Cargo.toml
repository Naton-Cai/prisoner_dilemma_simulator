
[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { workspace = true }
PrisonerDilemmaSimulator = { path = "../game", optional = true }

[features]
default = ["PrisonerDilemmaSimulator"]
dylib = ["fyrox/dylib"]
